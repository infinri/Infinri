<?php
/**
 * Forgot Password Template
 * Context-aware: adapts for customer vs admin
 */

$this->layout('layouts/one-column');
$this->setTitle('Reset Password');
?>

<?php $this->startBlock('head_css') ?>
<link rel="stylesheet" href="<?= $this->moduleAsset('Auth', 'base/web/css/auth.css') ?>">
<?php $this->endBlock() ?>

<?php $this->startBlock('main') ?>
<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <?php if ($isAdmin ?? false): ?>
                <span class="badge auth-badge-admin">Admin Portal</span>
            <?php endif; ?>
            <h1 class="auth-title">Reset Password</h1>
            <p class="auth-subtitle">Enter your email to receive a reset link</p>
        </div>

        <?php if ($status = $this->flash('status')): ?>
            <div class="alert alert-success" role="alert">
                <?= e($status) ?>
            </div>
        <?php endif; ?>

        <?php if ($error = $this->flash('error')): ?>
            <div class="alert alert-error" role="alert">
                <?= e($error) ?>
            </div>
        <?php endif; ?>

        <form method="POST" action="<?= ($isAdmin ?? false) ? '/admin/forgot-password' : '/forgot-password' ?>" class="auth-form" data-auth-form>
            <?= csrf_field() ?>

            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    class="form-input" 
                    value="<?= e($this->old('email', '')) ?>"
                    required 
                    autofocus
                    autocomplete="email"
                >
            </div>

            <button type="submit" class="btn btn-primary btn-block" data-submit-btn>
                Send Reset Link
            </button>
        </form>

        <div class="auth-footer">
            <a href="<?= ($isAdmin ?? false) ? '/admin/login' : '/login' ?>" class="auth-link">
                ← Back to login
            </a>
        </div>
    </div>
</div>
<?php $this->endBlock() ?>

<?php $this->startBlock('footer_js') ?>
<script src="<?= $this->moduleAsset('Auth', 'base/web/js/auth.js') ?>"></script>
<?php $this->endBlock() ?>
