<?php
/**
 * Reset Password Template
 * Shows form to set new password after clicking reset link
 * 
 * Available variables:
 * - $token: string - reset token
 * - $email: string - user email
 */

$this->layout('layouts/one-column');
$this->setTitle('Set New Password');
?>

<?php $this->startBlock('head_css') ?>
<link rel="stylesheet" href="<?= $this->moduleAsset('Auth', 'base/web/css/auth.css') ?>">
<?php $this->endBlock() ?>

<?php $this->startBlock('main') ?>
<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h1 class="auth-title">Set New Password</h1>
            <p class="auth-subtitle">Choose a strong password for your account</p>
        </div>

        <?php if ($errors = $this->flash('errors')): ?>
            <div class="alert alert-error" role="alert">
                <ul class="alert-list">
                    <?php foreach ($errors as $field => $fieldErrors): ?>
                        <?php foreach ((array)$fieldErrors as $error): ?>
                            <li><?= e($error) ?></li>
                        <?php endforeach; ?>
                    <?php endforeach; ?>
                </ul>
            </div>
        <?php endif; ?>

        <?php if ($error = $this->flash('error')): ?>
            <div class="alert alert-error" role="alert">
                <?= e($error) ?>
            </div>
        <?php endif; ?>

        <form method="POST" action="/reset-password" class="auth-form" data-auth-form>
            <?= csrf_field() ?>
            <input type="hidden" name="token" value="<?= e($token ?? '') ?>">

            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    class="form-input" 
                    value="<?= e($email ?? $this->old('email', '')) ?>"
                    required
                    autocomplete="email"
                >
            </div>

            <div class="form-group">
                <label for="password" class="form-label">New Password</label>
                <div class="form-input-toggle-wrapper">
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        class="form-input" 
                        required
                        autocomplete="new-password"
                        minlength="12"
                        data-password-strength
                    >
                    <button type="button" class="form-input-toggle" data-toggle-password aria-label="Toggle password visibility">
                        <span class="icon-eye"></span>
                    </button>
                </div>
                <div class="password-strength" data-password-strength-meter hidden>
                    <div class="password-strength-bar"></div>
                    <span class="password-strength-text"></span>
                </div>
                <p class="form-help">Minimum 12 characters with uppercase, lowercase, numbers, and symbols</p>
            </div>

            <div class="form-group">
                <label for="password_confirmation" class="form-label">Confirm New Password</label>
                <div class="form-input-toggle-wrapper">
                    <input 
                        type="password" 
                        id="password_confirmation" 
                        name="password_confirmation" 
                        class="form-input" 
                        required
                        autocomplete="new-password"
                    >
                    <button type="button" class="form-input-toggle" data-toggle-password aria-label="Toggle password visibility">
                        <span class="icon-eye"></span>
                    </button>
                </div>
            </div>

            <button type="submit" class="btn btn-primary btn-block" data-submit-btn>
                Reset Password
            </button>
        </form>

        <div class="auth-footer">
            <a href="/login" class="auth-link">← Back to login</a>
        </div>
    </div>
</div>
<?php $this->endBlock() ?>

<?php $this->startBlock('footer_js') ?>
<script src="<?= $this->moduleAsset('Auth', 'base/web/js/auth.js') ?>"></script>
<?php $this->endBlock() ?>
