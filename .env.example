# Environment Configuration Template
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control

# Application Environment (development/production)
APP_ENV=production

# Site Configuration
SITE_NAME=
SITE_URL=

# Security
CSRF_ENABLED=true
HTTPS_ONLY=true

# SMTP Email Configuration (for contact form)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
# Note: Spaces in password are automatically removed (Gmail app passwords have spaces)
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Portfolio Contact Form
# Note: Customer name will be shown as sender (e.g., "John Doe (via Portfolio)")
SMTP_RECIPIENT_EMAIL=your-email@gmail.com
SMTP_RECIPIENT_NAME=Your Name
